# Maximum-k-Colorable-Subgraph
Given a graph and a fixed number of colors k, find the largest induced subgraph that can be properly k-colored. It's NP-hard in general, which is why it will be solved via SAT encoding
